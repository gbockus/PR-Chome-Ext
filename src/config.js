let storageKey = 'gh-pr-mon';
const config = {
  githubApiKey: `${storageKey}-gh-api-key`,
  githubUserName: `${storageKey}-gh-username`,
  githubPassword: `${storageKey}-gp-password`,
  repositories: `${storageKey}-repos`
};